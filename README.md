# 🎴 TACTIX // BLACKBOX (Flask Backend + API + Stats)

Strategic Board Game Engine using Game Trees & Minimax.

## Run
```bash
python -m venv .venv
# Windows:
.venv\Scripts\activate
# mac/linux:
# source .venv/bin/activate

pip install -r requirements.txt
python app.py
